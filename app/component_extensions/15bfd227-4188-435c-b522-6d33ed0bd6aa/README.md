## SAM3 模型加载器

加载 SAM3 统一模型。该模型同时支持“图像分割”和“视频对象跟踪”。

### ⚙️ 属性参数

| 参数名 | 说明 |
| :--- | :--- |
| **model_path** | 默认为 `models/sam3/sam3.pt`。如果文件不存在，组件会**自动从 HuggingFace 下载** (约 3-4GB)。 |

### 📦 输出
*   **SAM3_MODEL**: 统一模型对象，连接到 `SAM3统一分割器` 使用。